{
  "name": "ironwatch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r --parallel run start",
    "postinstall": "pnpm --filter @ironwatch/db run generate"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
